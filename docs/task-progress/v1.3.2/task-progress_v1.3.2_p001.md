# v1.3.2

- TASK-0009: Defer the five upstream ravf debayer cv2 imports in a staged package; exclude OpenCV from the frozen raw-frame application. Build-only delivery, no application execution or runtime tests.
